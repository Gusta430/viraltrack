{
  "name": "viraltrack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "build": "apt-get update -qq && apt-get install -y -qq ffmpeg >/dev/null 2>&1 || echo 'apt-get skipped — using ffmpeg-static'"
  },
  "dependencies": {
    "audio-decode": "^3.3.0",
    "essentia.js": "^0.1.3",
    "ffmpeg-static": "^5.2.0"
  }
}
